# Green 애플리케이션을 위한 Dockerfile
FROM alpine:3.18

# 필요한 패키지 설치
RUN apk add --no-cache curl

# 애플리케이션 바이너리 복사 (실제 대회에서는 제공된 바이너리 사용)
COPY green /app/green

# 실행 권한 부여
RUN chmod +x /app/green

# 작업 디렉토리 설정
WORKDIR /app

# 애플리케이션 실행
EXPOSE 8080
CMD ["/app/green"]
